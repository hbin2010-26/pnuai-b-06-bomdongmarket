services:
  db:
    image: mysql:8.4
    container_name: farmbroker-db
    environment:
      # 값은 .env 에서 주입 (없으면 compose 가 에러로 멈춤 — 커밋 파일엔 secret 미포함)
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:?DB_PASSWORD 를 .env 에 설정하세요 (cp .env.example .env)}
      MYSQL_DATABASE: farmbroker
      TZ: Asia/Seoul
    ports:
      # 로컬 MySQL(3306)과 충돌하지 않도록 호스트는 3307 사용
      # Workbench 등에서 localhost:3307 로 접속
      - "3307:3306"
    volumes:
      - farmbroker-mysql-data:/var/lib/mysql
      # 최초 실행(볼륨이 비어 있을 때)에만 이 폴더의 *.sql 이 자동 실행됨 — 시드 데이터 공유용
      - ./db/init:/docker-entrypoint-initdb.d
    healthcheck:
      # 초기화 중 임시 서버는 TCP를 받지 않으므로 127.0.0.1(TCP) 핑으로 준비 완료를 판별
      test: ["CMD-SHELL", "mysqladmin ping -h127.0.0.1 -uroot -p\"$$MYSQL_ROOT_PASSWORD\" --silent"]
      interval: 5s
      timeout: 5s
      retries: 20

  backend:
    build: ./farmbroker
    container_name: farmbroker-api
    ports:
      - "8080:8080"
    # application.yml은 환경변수 이름만 정의하고 실제 값은 커밋되지 않는 .env에서 주입합니다.
    env_file:
      - .env
    environment:
      # 컨테이너 OS 시간대를 db 서비스와 통일한다(로그·shell date 등 OS 레벨 일관성).
      # JVM 기본 시간대는 코드(ApplicationTimeZone)에서 KST로 고정하므로 이 값이 없어도 감사 시각은 안전하다. (issue #71)
      TZ: ${TZ:-Asia/Seoul}
      # .env에 DB_URL이 없던 기존 개발 환경도 컨테이너 DB를 사용하도록 기본값을 둡니다.
      DB_URL: ${DB_URL:-jdbc:mysql://db:3306/farmbroker?serverTimezone=Asia/Seoul&characterEncoding=UTF-8}
      # 업로드 이미지는 컨테이너 밖 볼륨에 두어야 재시작·재빌드 후에도 남는다.
      FILE_UPLOAD_DIR: ${FILE_UPLOAD_DIR:-/var/farmbroker/uploads}
    volumes:
      - farmbroker-uploads:/var/farmbroker/uploads
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  frontend:
    build:
      context: ./farmbroker-web
      args:
        # Vite는 빌드 시점에 값을 번들에 박으므로 런타임 environment로는 전달되지 않는다.
        # compose가 .env에서 읽은 값을 그대로 build arg로 넘긴다.
        # 키를 바꾸면 docker compose up --build 로 이미지를 다시 만들어야 반영된다.
        VITE_KAKAO_MAP_APP_KEY: ${VITE_KAKAO_MAP_APP_KEY:-}
    container_name: farmbroker-web
    ports:
      # CORS 허용 Origin(http://localhost:5173)과 맞추기 위해 5173으로 매핑
      - "5173:80"
    depends_on:
      - backend

volumes:
  farmbroker-mysql-data:
  farmbroker-uploads:
