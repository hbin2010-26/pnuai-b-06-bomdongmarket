# 운영 브라우저는 Vercel의 동일 출처 /api rewrite를 통해 Render API에 연결합니다.
# Vercel Production 환경변수가 설정되어 있으면 해당 값이 우선합니다.
VITE_USE_MOCKS=false
VITE_API_BASE_URL=/api
# WebSocket은 Vercel rewrite를 통하지 않고 Render에 직접 연결하고 STOMP 티켓으로 인증합니다.
VITE_WS_URL=wss://farmbroker.onrender.com/api/ws-chat
